/*

// useMem0
--> useMemo is a react hook that lets you cache a result of a calculation between re-renders.
    syntax: 
    useMemo(()=>{
        //code
    },[dependencies])


//useCallback
--> useCallback is a react hook that lets you cache a function definition between re-renders.
    syntax:
    useCallback(()=>{
        //code
    },[dependencies])


//useRef
--> useRef is a react hook that let's you reference a value that's not need for rendering
    syntax:
    const x = useRef(initialValue)
*/